<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ===== Console Appender (مع تفاصيل أكتر) ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{40} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===== File Appender ===== -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===== Logger for EventBus ===== -->
    <logger name="com.genesis.p2p.events" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for Alerts ===== -->
    <logger name="com.genesis.p2p.events.alerts" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for Peer Connection (DEBUG for full details) ===== -->
    <logger name="com.genesis.p2p.core.peer" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for Transport (DEBUG to see all connections) ===== -->
    <logger name="com.genesis.p2p.transport" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for TCP Transport (DEBUG for connection details) ===== -->
    <logger name="com.genesis.p2p.transport.tcp" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for Discovery (DEBUG to see discovered IPs) ===== -->
    <logger name="com.genesis.p2p.discovery" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for NAT ===== -->
    <logger name="com.genesis.p2p.nat" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Logger for Security ===== -->
    <logger name="com.genesis.p2p.security" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- ===== Root Logger ===== -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
